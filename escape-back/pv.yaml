apiVersion: v1
kind: PersistentVolume
metadata:
  name: media-pv
  namespace: papello
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/mnt/data"  # This is an example path on the host machine
